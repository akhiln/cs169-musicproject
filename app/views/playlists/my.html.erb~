<h2> My Playlists </h2>

<% if @songs.size > 0 %>
  <% @songs.each do |s| %>
    <%= link_to "#{s.name}", "/songs/show/#{s.id}" %>
  <% end %>
<% else %>
  You haven't created any playlists yet! 
<% end %>

