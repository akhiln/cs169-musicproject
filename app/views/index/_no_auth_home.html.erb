<h1>Welcome to Project Jukebox!</h1>

(put fancy shiny graphics here, maybe screen caps once we have a pretty style)</p>
<% @actions.each do |a| %>
  <%= sprintf("%d just  %sd  %s   %d", a.user_id, a.action.to_s, a.obj_type.to_s, a.obj_id) %>
  </p>
<% end %>
